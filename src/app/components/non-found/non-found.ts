import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-non-found',
  imports: [RouterLink],
  templateUrl: './non-found.html',
  styleUrl: './non-found.scss',
})
export class NonFound {}
